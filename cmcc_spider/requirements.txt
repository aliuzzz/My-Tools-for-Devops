# CDN爬虫脚本依赖清单
# 安装方式: pip install -r requirements.txt

# 浏览器自动化（v2/v4/v4.1共用）
selenium>=4.0.0

# HTTP请求（cookie直下载 + 推送接口）
requests>=2.28.0

# 验证码自动识别（v2/v4/v4.1共用）
ddddocr>=1.4.0
pillow>=9.0.0
numpy>=1.21.0
onnxruntime>=1.12.0

# YAML配置解析（v4/v4.1需要）
pyyaml>=6.0

# XLS解析 + 采样推送（v4.1需要）
pandas>=1.5.0
xlrd>=2.0.0
